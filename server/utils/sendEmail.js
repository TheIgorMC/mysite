// Utility for sending emails (placeholder, use nodemailer in production)
module.exports = async function sendEmail({ to, subject, html }) {
  // Implement with nodemailer or similar
  console.log(`Send email to ${to}: ${subject}`);
};
